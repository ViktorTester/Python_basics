n = int(input())
arr = []
for i in range(n):
    s = input()
    arr.append(s)
print(arr)


# The input is a natural number n, and then n strings.
# The program creates a list from the specified strings and displays it.
